package cs361.battleships.models;


//Prototyping done by Nolan Nichols
public class Fleet {
    /*
    private int numShipsInFleet = 0;
    private int moveCount = 0;

    //Set the number of ships in the fleet
    public void setNumShipsInFleet(int myNumShips){

        numShipsInFleet = myNumShips;

    }

    //Get number of ships in fleet
    public int getNumShipsInFleet(){

        return numShipsInFleet;

    }

    //Checks if a fleet can move
    public boolean canFleetMove(){

        if(moveCount < 2){
            return true;
        }
        else{
            return false;
        }

    }

    //Increments the move counter
    public void move(){
        if(canFleetMove()){
            moveCount++;
        }
        else{
            //console.log("You have already moved the fleet twice\n");
        }

    }

    public void moveDirection(char direction, Class Ship){
        //List <Ship> shipsLocation =  Ship.getOccupiedSquares;

        if(direction == 'n'){

        }
        //Run 2D array to move ship in appropriate direction




    }

    public boolean isOnEdge(Class Square){

        if(Square.isOutOfBounds()){
            return true;
        }
        else{
            return false;
        }
    }

    public boolean areShipsOverlapped(Class Ship){
        //for each ship, check for overlapping
        //Use for loop here...
    }

   */

}
